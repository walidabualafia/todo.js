{
  "name": "bloom",
  "version": "1.0.0",
  "description": "Bloom - A beautiful, project-based todo list application",
  "scripts": {
    "server": "cd server && node server.js",
    "client": "cd client && npm start",
    "install-all": "npm install && cd server && npm install && cd ../client && npm install",
    "dev": "concurrently \"npm run server\" \"npm run client\""
  },
  "keywords": ["todo", "task-manager", "project-management", "react", "nodejs"],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
